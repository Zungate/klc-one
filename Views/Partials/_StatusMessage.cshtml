@model string

@if (!String.IsNullOrEmpty(Model))
{
    var statusMessageClass = Model.StartsWith("Fejl") ? "danger" : "success";
    @*<div class="alert alert-@statusMessageClass alert-dismissible" role="alert">
        @Model
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>

        </div>*@
    <div class="toast-container position-absolute top-0 end-0 p-3">
        <div class="toast toast-demo d-flex align-items-center text-white bg-@statusMessageClass border-0" role="alert" aria-live="assertive" aria-atomic="true">
            <div class="toast-body">
                @Model
            </div>
            <button type="button" class="btn-close btn-close-white ms-auto me-2" data-bs-dismiss="toast" aria-label="Close"></button>
        </div>
    </div>
}
